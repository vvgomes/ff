= form_for(@accomplishment) do |f|
  p Congratulate #{f.collection_select :receiver_id, current_user.peers, :id, :username, :prompt => '[your peer]'} for:
  = f.text_area :description, :placeholder => 'doing something awesome…', :autofocus => true
  p #{f.collection_select :group_id, Group.all, :id, :name, :prompt => 'Where?' } #{f.submit :value => 'Feedback it!'}
